<div class="container">
  <h1>Articles (<%= link_to "new", new_path  %>)</h1>
  <% @articles.each do |article| %>
    <div class="card">
      <h2><%= link_to article.title, article_path(article.id) %></h2>
      <p><%= article.content %></p>
    </div>
  <% end %>
</div>
